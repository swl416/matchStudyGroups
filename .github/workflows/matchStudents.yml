on:
  workflow_dispatch:
   
jobs:
  match:
    name: match students
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: run python script
      run: |
        python matchingProcess.py
